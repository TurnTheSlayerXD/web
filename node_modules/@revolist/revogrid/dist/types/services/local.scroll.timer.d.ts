import type { DimensionType } from "../types/index";
/**
 * Apply changes only if mousewheel event happened some time ago (scrollThrottling)
 */
export declare class LocalScrollTimer {
    private scrollThrottling;
    /**
     * Last mw event time for trigger scroll function below
     * If mousewheel function was ignored we still need to trigger render
     */
    private mouseWheelScrollTimestamp;
    private lastKnownScrollCoordinate;
    constructor(scrollThrottling?: number);
    setCoordinate(e: {
        dimension: DimensionType;
        coordinate: number;
    }): void;
    /**
     * Remember last mw event time
     */
    latestScrollUpdate(dimension: DimensionType): void;
    /**
     * Check if scroll is ready to accept new value
     */
    isReady(type: DimensionType, coordinate: number): boolean;
}
